package online.educoreng.educore

import com.journeyapps.barcodescanner.CaptureActivity

/** Keeps the attendance scanner in the app's supported portrait layout. */
class PortraitCaptureActivity : CaptureActivity()
