<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Parallel Curriculum Result</title>
<style>
@page{margin:20mm 15mm}*{box-sizing:border-box}body{font-family:DejaVu Sans,sans-serif;color:#172033;font-size:10px;margin:0}.header{text-align:center;border-bottom:2px solid #102A56;padding-bottom:9px;margin-bottom:12px}.school{font-size:17px;font-weight:700;color:#102A56}.programme{font-size:12px;font-weight:700;margin-top:4px}.period{font-size:9px;color:#667085;margin-top:3px}.meta{width:100%;border-collapse:collapse;margin-bottom:12px}.meta td{width:25%;padding:7px;border:1px solid #DDE3EC;vertical-align:top}.label{display:block;font-size:7.5px;color:#667085;text-transform:uppercase;margin-bottom:2px}.value{font-weight:700;color:#102A56}.result{width:100%;border-collapse:collapse;margin-top:4px}.result th{background:#102A56;color:#fff;font-size:8px;padding:6px 5px;border:1px solid #102A56;text-align:center}.result th:first-child,.result td:first-child{text-align:left}.result td{padding:6px 5px;border:1px solid #DDE3EC;text-align:center;font-size:8.5px}.summary{width:100%;border-collapse:separate;border-spacing:5px;margin-top:11px}.summary td{border:1px solid #DDE3EC;padding:7px;text-align:center}.summary strong{display:block;color:#102A56;font-size:12px}.summary span{font-size:7.5px;color:#667085}.status{margin-top:10px;padding:7px;border:1px solid #DDE3EC;background:#F8FAFC;font-size:8px}.footer{margin-top:16px;padding-top:8px;border-top:1px solid #DDE3EC;color:#667085;font-size:7.5px;text-align:center}
</style>
</head>
<body>
@php($row=$report['student_result'])
<div class="header">
    <div class="school">{{ auth()->user()->tenant?->name ?? config('app.name') }}</div>
    <div class="programme">{{ $report['curriculum']?->name }} — Student Result</div>
    <div class="period">{{ $report['term']->session?->name }} · {{ $report['term']->name }} · {{ $report['class']->name }}</div>
</div>

<table class="meta">
    <tr>
        <td><span class="label">Student</span><span class="value">{{ $row['student']->full_name }}</span></td>
        <td><span class="label">Admission No.</span><span class="value">{{ $row['student']->admission_number }}</span></td>
        <td><span class="label">Parallel Class</span><span class="value">{{ $report['class']->name }}</span></td>
        <td><span class="label">Result Status</span><span class="value">{{ $report['is_published'] ? 'Published' : 'Draft' }}</span></td>
    </tr>
</table>

<table class="result">
    <thead>
        <tr>
            <th>Subject</th>
            @foreach($report['components'] as $component)
                <th>{{ $component->name }} /{{ number_format($component->weight_percentage,0) }}</th>
            @endforeach
            <th>Total %</th>
            <th>Grade</th>
            <th>Remark</th>
        </tr>
    </thead>
    <tbody>
        @foreach($row['subjects'] as $subject)
            <tr>
                <td><strong>{{ $subject['subject'] }}</strong></td>
                @foreach($report['components'] as $component)
                    @php($componentRow=collect($subject['components'])->firstWhere('id',(int)$component->id))
                    <td>{{ $componentRow && $componentRow['score'] !== null ? number_format($componentRow['score'],1) : '—' }}</td>
                @endforeach
                <td>{{ $subject['percentage'] === null ? '—' : number_format($subject['percentage'],1) }}</td>
                <td>{{ $subject['grade'] ?: '—' }}</td>
                <td>{{ $subject['remark'] ?: ($subject['complete'] ? '—' : 'Incomplete') }}</td>
            </tr>
        @endforeach
    </tbody>
</table>

<table class="summary">
    <tr>
        <td><strong>{{ $row['completed_subject_count'] }}/{{ $row['subject_count'] }}</strong><span>Subjects Complete</span></td>
        <td><strong>{{ number_format($row['grand_total'],1) }}/{{ number_format($row['maximum_total'],0) }}</strong><span>Total</span></td>
        <td><strong>{{ $row['average'] === null ? '—' : number_format($row['average'],2).'%' }}</strong><span>Average</span></td>
        <td><strong>{{ $row['position'] ?: '—' }}</strong><span>Position</span></td>
        <td><strong>{{ $row['failed_subjects'] }}</strong><span>Failed Subjects</span></td>
    </tr>
</table>

<div class="status">
    {{ $row['complete'] ? 'All active parallel-curriculum subjects are complete for this student.' : 'This result is incomplete and remains subject to further score entry.' }}
</div>

<div class="footer">Generated by EduCore · Standalone parallel curriculum result</div>
</body>
</html>
