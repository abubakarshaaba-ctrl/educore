name: Build and Deploy Native Android App

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - mobile-overhaul
      - codex/native-login-brand-hotfix
    paths:
      - 'mobile-native/**'
      - 'educore/app/Console/Commands/MigrateSensitiveDocuments.php'
      - 'educore/app/Http/Controllers/Api/**'
      - 'educore/app/Http/Controllers/AdmissionController.php'
      - 'educore/app/Http/Controllers/PushNotificationController.php'
      - 'educore/app/Http/Controllers/RecruitmentController.php'
      - 'educore/app/Http/Controllers/ReportCardComputeController.php'
      - 'educore/app/Http/Controllers/ReportCardPublicationController.php'
      - 'educore/app/Http/Controllers/ReportCardPdfController.php'
      - 'educore/app/Http/Controllers/SelfDeployController.php'
      - 'educore/app/Http/Controllers/SelfWorkflowValidationController.php'
      - 'educore/app/Http/Controllers/StudentTransferController.php'
      - 'educore/app/Http/Controllers/StudentClassTransferController.php'
      - 'educore/app/Http/Middleware/**'
      - 'educore/app/Models/AcademicTrack.php'
      - 'educore/app/Models/AdmissionDocument.php'
      - 'educore/app/Models/JobApplicantDocument.php'
      - 'educore/app/Models/StudentTransfer.php'
      - 'educore/app/Models/StudentClassTransfer.php'
      - 'educore/app/Services/Auth/LoginUserResolver.php'
      - 'educore/app/Services/CrossSchoolStudentTransferService.php'
      - 'educore/app/Services/ReportCardComputationService.php'
      - 'educore/app/Services/ReportCardPublicationService.php'
      - 'educore/app/Services/ReportCardDocumentService.php'
      - 'educore/app/Services/StudentClassTransferService.php'
      - 'educore/app/Services/Mobile/MobileDashboardService.php'
      - 'educore/app/Services/Mobile/MobileModuleService.php'
      - 'educore/app/Services/PayrollGenerationService.php'
      - 'educore/app/Services/SensitiveDocumentStorage.php'
      - 'educore/app/Services/TenantUrlGenerator.php'
      - 'educore/database/migrations/**'
      - 'educore/resources/views/dashboard/staff.blade.php'
      - 'educore/resources/views/students/class-transfers/**'
      - 'educore/resources/views/portal/admissions/success.blade.php'
      - 'educore/routes/api.php'
      - 'educore/routes/deploy.php'
      - 'educore/routes/mobile-*.php'
      - 'educore/routes/web-report-compute.php'
      - 'educore/routes/web-report-publication.php'
      - 'educore/routes/web-report-pdf.php'
      - 'educore/bootstrap/app.php'
      - 'educore/tests/Feature/Mobile*.php'
      - 'educore/tests/Feature/ReportCardPublicationServiceTest.php'
      - 'educore/tests/Feature/ReportCardDocumentServiceTest.php'
      - 'educore/tests/Feature/WebReportComputeRouteTest.php'
      - 'educore/tests/Feature/WebReportPublicationRouteTest.php'
      - 'educore/tests/Feature/WebReportPdfRouteTest.php'
      - 'educore/tests/Feature/IntraClassTransferTest.php'
      - 'educore/tests/Feature/SensitiveDocumentStorageTest.php'
      - 'educore/tests/Feature/StudentClassTransferTest.php'
      - 'educore/tests/Feature/WebSecurityHardeningTest.php'
      - 'educore/tests/Feature/WebPushSecurityTest.php'
      - 'educore/tests/Feature/PublicDiagnosticBoundaryTest.php'
      - 'educore/tests/Feature/TenantHostResolutionTest.php'
      - 'educore/tests/Feature/AcademicRepositoryReaderTest.php'
      - 'educore/tests/Feature/CbtRestructuringTest.php'
      - 'educore/tests/Feature/PushRegistrationTest.php'
      - 'educore/tests/Feature/TenantAccessEnforcementTest.php'
      - 'educore/tests/Feature/SelfWorkflowValidationControllerTest.php'
      - '.htaccess'
      - 'educore/public/.htaccess'
      - '.github/workflows/native-android.yml'

concurrency:
  group: native-android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      release_available: ${{ steps.signing.outputs.available }}
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/master' }}

      - name: Validate Laravel mobile and security contracts
        working-directory: educore
        run: |
          composer install --no-interaction --prefer-dist --no-progress
          cp .env.example .env
          php artisan key:generate
          DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan test \
            tests/Feature/MobileBootstrapTest.php \
            tests/Feature/MobileStaffCbtTest.php \
            tests/Feature/MobileStaffDirectoryTest.php \
            tests/Feature/MobileAnalyticsTest.php \
            tests/Feature/MobileExportsTest.php \
            tests/Feature/MobileRiskTest.php \
            tests/Feature/MobileAdmissionsTest.php \
            tests/Feature/MobileSpecialistOperationsTest.php \
            tests/Feature/MobileLibraryTest.php \
            tests/Feature/MobileInventoryTest.php \
            tests/Feature/MobileHostelTest.php \
            tests/Feature/MobileFeesTest.php \
            tests/Feature/MobilePayrollTest.php \
            tests/Feature/MobileExpensesTest.php \
            tests/Feature/MobileSubjectsTest.php \
            tests/Feature/MobileCurriculumTest.php \
            tests/Feature/MobileAcademicCycleTest.php \
            tests/Feature/MobileSkillsTest.php \
            tests/Feature/MobileGradebookTest.php \
            tests/Feature/MobileReportsTest.php \
            tests/Feature/MobileReportComputationTest.php \
            tests/Feature/MobileTransfersTest.php \
            tests/Feature/MobilePlatformTest.php \
            tests/Feature/MobileOperationsTest.php \
            tests/Feature/ReportCardPublicationServiceTest.php \
            tests/Feature/ReportCardDocumentServiceTest.php \
            tests/Feature/WebReportComputeRouteTest.php \
            tests/Feature/WebReportPublicationRouteTest.php \
            tests/Feature/WebReportPdfRouteTest.php \
            tests/Feature/IntraClassTransferTest.php \
            tests/Feature/SensitiveDocumentStorageTest.php \
            tests/Feature/StudentClassTransferTest.php \
            tests/Feature/WebSecurityHardeningTest.php \
            tests/Feature/WebPushSecurityTest.php \
            tests/Feature/PublicDiagnosticBoundaryTest.php \
            tests/Feature/TenantHostResolutionTest.php \
            tests/Feature/SelfWorkflowValidationControllerTest.php

      - name: Build and test debug app
        working-directory: mobile-native
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon --no-parallel \
            :core:common:testDebugUnitTest \
            :core:designsystem:testDebugUnitTest \
            :core:model:testDebugUnitTest \
            :core:network:testDebugUnitTest \
            :core:security:testDebugUnitTest \
            :core:data:testDebugUnitTest \
            :app:testDebugUnitTest \
            :app:lintDebug \
            :app:assembleDebug

      - name: Prepare protected release signing
        id: signing
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_STORE_PASSWORD: ${{ secrets.KEYSTORE_STORE_PASSWORD }}
          KEYSTORE_KEY_PASSWORD: ${{ secrets.KEYSTORE_KEY_PASSWORD }}
          KEYSTORE_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ] || [ -z "$KEYSTORE_STORE_PASSWORD" ] || [ -z "$KEYSTORE_KEY_PASSWORD" ] || [ -z "$KEYSTORE_KEY_ALIAS" ]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "Release signing secrets are unavailable; debug validation is still complete."
            exit 0
          fi

          mkdir -p mobile-native/.ci-signing
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > mobile-native/.ci-signing/educore-release.jks
          {
            echo 'storeFile=educore-release.jks'
            echo "storePassword=$KEYSTORE_STORE_PASSWORD"
            echo "keyAlias=$KEYSTORE_KEY_ALIAS"
            echo "keyPassword=$KEYSTORE_KEY_PASSWORD"
          } > mobile-native/.ci-signing/release.properties
          echo "available=true" >> "$GITHUB_OUTPUT"

      - name: Build signed release app
        if: steps.signing.outputs.available == 'true'
        working-directory: mobile-native
        run: |
          ./gradlew --no-daemon --no-parallel \
            -PEDUCORE_RELEASE_PROPERTIES=.ci-signing/release.properties \
            :app:lintRelease \
            :app:assembleRelease
          cp app/build/outputs/apk/release/app-release.apk ../EduCore.apk

      - name: Verify signed release
        if: steps.signing.outputs.available == 'true'
        shell: bash
        run: |
          APK_SIGNER="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -1)"
          test -n "$APK_SIGNER"
          "$APK_SIGNER" verify --verbose --print-certs EduCore.apk
          sha256sum EduCore.apk > EduCore.apk.sha256

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: educore-native-debug-${{ github.sha }}
          path: mobile-native/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14

      - name: Upload signed release APK
        uses: actions/upload-artifact@v4
        if: steps.signing.outputs.available == 'true'
        with:
          name: educore-native-release-${{ github.sha }}
          path: |
            EduCore.apk
            EduCore.apk.sha256
          retention-days: 30

  deploy:
    needs: validate
    if: github.ref == 'refs/heads/master' && needs.validate.outputs.release_available == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: educore-native-release-${{ github.sha }}

      - name: Publish APK to the live server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          source: EduCore.apk
          target: ${{ secrets.DEPLOY_APK_PATH }}

      - name: Verify live APK file
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            test -s "${{ secrets.DEPLOY_APK_PATH }}/EduCore.apk"
            stat -c 'Published EduCore.apk: %s bytes' "${{ secrets.DEPLOY_APK_PATH }}/EduCore.apk"
